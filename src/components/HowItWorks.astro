---
import { copy, type Lang } from '../i18n/copy';

interface Props {
	lang?: Lang;
}

const { lang = 'en' } = Astro.props;
const t = copy[lang].howItWorks;
const steps = t.steps;
const howItWorksHref = lang === 'es' ? '/es/how-it-works/' : '/how-it-works/';
---

<section id="how" data-screen-label="How it works" class="how">
	<div class="wrap">
		<div class="reveal eyebrow">{t.eyebrow}</div>
		<h2 class="reveal">{t.h2}</h2>
		<div class="grid">
			{
				steps.map((st, i) => (
					<div class="reveal step" style={`transition-delay: ${i * 0.08}s;`}>
						<div class="num">{st.num}</div>
						<div class="title">{st.title}</div>
						<p>{st.body}</p>
					</div>
				))
			}
		</div>
		<div class="reveal closing">
			<div class="closing-text">{t.closingText}</div>
			<a href={howItWorksHref} class="btn-slide-gradient btn-slide-outline closing-cta" data-gtm-id="how_it_works_see_more">
				{t.closingCta}
			</a>
		</div>
	</div>
</section>

<style>
	.how {
		border-bottom: 1px solid var(--border);
		background: var(--white);
	}

	.wrap {
		max-width: 1100px;
		margin: 0 auto;
		padding: 100px 24px;
	}

	.eyebrow {
		font-size: 11px;
		letter-spacing: 0.22em;
		text-transform: uppercase;
		color: var(--muted);
		margin-bottom: 16px;
		font-weight: 500;
	}

	h2 {
		font-family: var(--font-display);
		font-size: clamp(36px, 5vw, 56px);
		font-weight: 800;
		line-height: 1.05;
		letter-spacing: -0.04em;
		margin: 0 0 64px;
		max-width: 600px;
	}

	.grid {
		display: grid;
		grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
		gap: 32px;
	}

	.step {
		border-top: 2px solid var(--ink);
		padding: 28px 0 8px 0;
	}

	.num {
		font-size: 14px;
		font-family: var(--font-mono);
		color: var(--accent);
		margin-bottom: 18px;
		font-weight: 500;
	}

	.title {
		font-size: 20px;
		font-weight: 500;
		margin-bottom: 12px;
		letter-spacing: -0.01em;
	}

	p {
		font-size: 15px;
		font-weight: 400;
		line-height: 1.6;
		color: var(--muted);
		margin: 0;
		text-wrap: pretty;
	}

	.closing {
		margin-top: 56px;
		padding-top: 32px;
		border-top: 1px solid var(--border);
		display: flex;
		flex-wrap: wrap;
		align-items: center;
		justify-content: space-between;
		gap: 24px;
	}

	.closing-text {
		font-size: 16px;
		line-height: 1.6;
		color: var(--muted);
		max-width: 480px;
		text-wrap: pretty;
	}

	.closing-cta {
		background: linear-gradient(to top, var(--ink) 50%, transparent 50%);
		border: 1px solid var(--ink);
		border-radius: 4px;
		padding: 14px 26px;
		color: var(--ink);
		font-size: 15px;
		font-weight: 500;
		flex: none;
	}
</style>
