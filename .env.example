# Optional. When unset, the waitlist form simulates a successful submission
# (matches the original design's behavior — no real backend exists yet).
# Point this at a real endpoint (e.g. the marta backend's /api/waitlist, once built)
# to POST { email } for real. Must be a public, CORS-enabled URL — this ships to the browser.
PUBLIC_WAITLIST_ENDPOINT=
